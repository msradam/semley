      ┓
┏┏┓┏┳┓┃┏┓┓┏
┛┗ ┛┗┗┗┗ ┗┫
          ┛
autonomous SRE investigation agent

surface host  /  plane node
read-only: every module here is a facts read; the reads run on the target host.
ledger: unkeyed SHA-256; every step and refusal is hash-chained.

inventory ~/semley/inventory/hosts.ini · name one of 
these hosts in your incident:
host  connection      
web1  ansible_host=orb

action space (read-only): service_facts  listen_ports_facts  package_facts  
setup
describe an incident · /help for commands · /quit to exit.

semley › the web service on web1 is not responding
posed target: `web1`  
Proposed scope: `web service not responding`

Please confirm I should investigate `web1` for that symptom.
semley › yes
  ▸ step triage target=web1 scope=web service not responding hypothesis=The web 
service is down, not listening on its expected port, or the host lacks 
network/service prerequisites.
  ▸ step read module=ansible.builtin.service_facts args={}
  ▸ step read module=ansible.builtin.listen_ports_facts args={}
  ▸ step read module=ansible.builtin.package_facts args={}
    read e1 ansible.builtin.service_facts on web1
    read e2 ansible.builtin.listen_ports_facts on web1
    read e3 ansible.builtin.package_facts on web1
  ▸ step conclude finding=nginx service on web1 is stopped and no TCP ports are 
listening cited_evidence=['e1', 'e2', 'e3']
    conclusion: confirmed on web1: nginx service on web1 is stopped and no TCP 
ports are listening
verdict: nginx on `web1` is stopped. In the service facts, both `nginx` and 
`nginx.service` are in `stopped` state (`e1`), the host has no TCP listening 
sockets at all (`e2`), and the nginx package is installed so this is not a 
missing-package issue (`e3`). Plain English: the web service is not responding 
because nginx is installed but not running, so nothing is listening for HTTP 
traffic on the host.
╭───────────────────────────────── confirmed ──────────────────────────────────╮
│ confirmed on web1: nginx service on web1 is stopped and no TCP ports are     │
│ listening                                                                    │
╰──────────────────────────────────────────────────────────────────────────────╯
evidence                                    
┏━━━━┳━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━┳━━━━━━━┓
┃ id ┃ read               ┃ target ┃ cited ┃
┡━━━━╇━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━╇━━━━━━━┩
│ e1 │ service_facts      │ web1   │   *   │
│ e2 │ listen_ports_facts │ web1   │   *   │
│ e3 │ package_facts      │ web1   │   *   │
└────┴────────────────────┴────────┴───────┘

/playbook saves the recorded Ansible playbook · describe the next incident 
(prior findings carry forward)

semley › /playbook
  playbook saved 
~/semley/.rocannon/playbooks/host_investigation.yml (3
tasks, credentials redacted)

semley › /quit
bye.
